#Definición de nuestro "contrato" de nuestra API

#<< Types >>

type Query {
    "Lista de los estudiantes de la academia"
    estudiantes: String

}

"Información de los estudiantes"
type Estudiante {
    "identificativo del estudiante"
    id: ID!
    nam: String!
    email: String!
    website: String
    course: [Course!]!
}

type Course {
    id: ID!
    title: String!
    description: String!
    clases: Int!
    time: Float!
    logo: String!
    level: Nivel!
    path: String!
    teacher: String!
    students: [Estudiante]
    reviews: [Valoracion!]!    
}

type Valoracion  {
    id: ID!
    name: String!
    points: Float!
    comment: String   
}

#<< Enums >>
enum Nivel {

    TODOS
    NOVATOS
    INTERMEDIO
    EXPERTO

}